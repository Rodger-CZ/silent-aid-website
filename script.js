function showMessage() {
  alert("Event closed (mock). Next step: connect this to your FastAPI backend.");
}

function handleSubmit(event) {
  event.preventDefault();
  alert("Message sent (mock). Next step: enable Netlify Forms or connect a backend endpoint.");
}
